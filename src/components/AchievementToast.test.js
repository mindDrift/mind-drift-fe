// import React from 'react';
// import { shallow } from 'enzyme';
// import AchievementToast from './AchievementToast';
// import withSessionMock from '../utils/WithSession';
// jest.mock('../utils/WithSession', () => ({}));

describe('AchievementToast component', () => {

  it('passes test that will always pass', () => {
    expect(true).toBeTruthy();
  });

  // it('renders AchievementToast with a new achievement', () => {
  //   const wrapper = shallow(<AchievementToast />);
  //   expect(wrapper).toMatchSnapshot();
  // });

  // it('renders nothing when there is no new achievement', () => {
  //   const wrapper = shallow(<AchievementToast />);
  //   expect(wrapper).toMatchSnapshot();
  // });
});
